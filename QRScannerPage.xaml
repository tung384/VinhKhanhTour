<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             xmlns:res="clr-namespace:OneSProject.Resources.Languages"
             x:Class="OneSProject.QRScannerPage"
             Title="{x:Static res:AppResources.QRHeader}">

    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="BarcodeReader"
                                       BarcodesDetected="OnBarcodesDetected" />
        
        <Border Stroke="#D4AF37" StrokeThickness="3" VerticalOptions="Center" 
                HorizontalOptions="Center" WidthRequest="250" HeightRequest="250"
                BackgroundColor="Transparent" InputTransparent="True">
            <Border.StrokeShape><RoundRectangle CornerRadius="20"/></Border.StrokeShape>
        </Border>

        <Label Text="{x:Static res:AppResources.QRInstruction}" 
               TextColor="White" HorizontalOptions="Center" 
               VerticalOptions="End" Margin="0,0,0,60" FontAttributes="Bold" />
    </Grid>
</ContentPage>