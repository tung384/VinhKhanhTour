<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:OneSProject.Models"
             xmlns:res="clr-namespace:OneSProject.Resources.Languages"
             x:Class="OneSProject.POIListPage" 
             x:DataType="local:POIListPage"
             Title="{x:Static res:AppResources.ListHeader}">
             
    <CollectionView x:Name="POICollection" 
                    SelectionMode="Single" 
                    SelectionChanged="OnSelectionChanged" 
                    Margin="10">

        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:POI">
                <Border StrokeShape="RoundRectangle 15" 
                        Padding="10" 
                        Margin="0,5" 
                        BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}"
                        Stroke="{AppThemeBinding Light=#E0E0E0, Dark=#333333}">
                    <Grid ColumnDefinitions="100, *" ColumnSpacing="15">
                        <Image Source="{Binding MainImage}" 
                               HeightRequest="80" 
                               WidthRequest="80" 
                               Aspect="AspectFill" />
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light=Black, Dark=White}"/>
                            <Label Text="{x:Static res:AppResources.TapToView}" FontSize="12" TextColor="Gray" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>

</ContentPage>