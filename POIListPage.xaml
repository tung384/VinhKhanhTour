<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:OneSProject.Models"
             x:Class="OneSProject.POIListPage" 
             x:DataType="local:POIListPage"
             Title="Danh sách quán ăn">
             
    <CollectionView x:Name="POICollection" 
                    SelectionMode="Single" 
                    SelectionChanged="OnSelectionChanged" 
                    Margin="10">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:POI">
                <Border StrokeShape="RoundRectangle 15" Padding="10" Margin="0,5" BackgroundColor="White">
                    <Grid ColumnDefinitions="100, *" ColumnSpacing="15">
                        <Image Source="{Binding MainImage}" 
                               HeightRequest="80" 
                               WidthRequest="80" 
                               Aspect="AspectFill" />
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="Nhấn để xem chi tiết" FontSize="12" TextColor="Gray" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>