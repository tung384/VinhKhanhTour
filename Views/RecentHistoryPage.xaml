<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:OneSProject.Models"
             xmlns:res="clr-namespace:OneSProject.Resources.Languages"
             x:Class="OneSProject.Views.RecentHistoryPage"
             Title="{x:Static res:AppResources.Set_RecentPOI}"
             BackgroundColor="{AppThemeBinding Light=#F8F8F8, Dark=#121212}">

    <Grid RowDefinitions="Auto, *">
        <Label Text="{x:Static res:AppResources.WelcomeMsg}" 
               Margin="20,10" 
               TextColor="Gray" 
               FontSize="14" />

        <CollectionView x:Name="HistoryCollection" 
                        Grid.Row="1"
                        SelectionMode="Single" 
                        SelectionChanged="OnSelectionChanged" 
                        Margin="10">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:POI">
                    <Border StrokeShape="RoundRectangle 15" 
                            Padding="10" Margin="0,5" 
                            BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}"
                            Stroke="{AppThemeBinding Light=#E0E0E0, Dark=#333333}">
                        <Grid ColumnDefinitions="80, *" ColumnSpacing="15">
                            <Image Source="{Binding MainImage}" 
                                   HeightRequest="60" WidthRequest="60" 
                                   Aspect="AspectFill" />
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                <Label Text="{Binding Name}" 
                                       FontSize="16" FontAttributes="Bold" 
                                       TextColor="{AppThemeBinding Light=Black, Dark=White}"/>
                                <Label Text="{x:Static res:AppResources.TapToView}" 
                                       FontSize="11" 
                                       TextColor="#D4AF37" />
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>